<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Transfer Product</title>

    <!-- Tautan Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <!-- Tautan Google Fonts -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">
    <!-- Tautan Font Awesome untuk ikon -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Import Css CreateView -->
    <link rel="stylesheet" href="/css/createView.css">
    
</head>

<body class="container">
    <div class="header-title">
        <i class="fas fa-box-open fa-3x text-primary"></i>
        <h1 class="display-4 fw-bold">Create Transfer Product</h1>
    </div>

    <div class="form-container">
        <form action="/updateTransfer/<%= transfer._id %>" method="POST" class="row g-3">
            <div class="col-md-6">
                <div class="mb-3">
                    <label for="id" class="form-label">ID:</label>
                    <input type="text" class="form-control" id="id" name="id" value="<%= transfer.id %>" required>
                </div>

                <div class="mb-3">
                    <label for="refno" class="form-label">Ref No:</label>
                    <input type="text" class="form-control" id="refno" name="refno" value="<%= transfer.refno %>" required>
                </div>

                <div class="mb-3">
                    <label for="from" class="form-label">From:</label>
                    <input type="text" class="form-control" id="from" name="from" value="<%= transfer.from %>" required>
                </div>
            </div>

            <div class="col-md-6">
                <div class="mb-3">
                    <label for="to" class="form-label">To:</label>
                    <input type="text" class="form-control" id="to" name="to" value="<%= transfer.to %>" required>
                </div>

                <div class="mb-3">
                    <label for="note" class="form-label">Note:</label>
                    <input type="text" class="form-control" id="note" name="note" value="<%= transfer.note %>" required>
                </div>
            </div>

            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Update Transfer Product</button>
            </div>
        </form>
    </div>

</body>
</html>