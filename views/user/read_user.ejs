<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Baca Pengguna</title>

    <!-- Tautan Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">
    <div class="text-left mb-5">
      <h1 class="display-4 fw-bold">Baca Pengguna</h1>
    </div>

    <div>
        <p><strong>Nama:</strong> <%= user.name %></p>
        <p><strong>Posisi:</strong> <%= user.position %></p>
        <p><strong>Kantor:</strong> <%= user.office %></p>
        <p><strong>Umur:</strong> <%= user.age %></p>
        <p><strong>Tanggal Mulai:</strong> <%= user.startDate.toISOString().split('T')[0] %></p>
        <p><strong>Gaji:</strong> Rp.<%= user.salary %></p>

        <h3>Produk yang Dimiliki</h3>
        <ul>
            <% if (user.products && user.products.length > 0) { %>
              <% user.products.forEach(product => { %>
                <!-- <li><%= product.sku %></li> -->
                <li><%= product.name %></li>
              <% }); %>
            <% } else { %>
              <li>User tidak memiliki produk.</li>
            <% } %>
          </ul>
    </div>

    <a href="/user" class="btn btn-primary mt-3">Back to Product List</a>

    <!-- Script Bootstrap JS dan Popper.js (letakkan di akhir body untuk performa yang lebih baik) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>